# Performance baselines

This repository is deliberately minimal and not performance-sensitive. The baseline expectation is:

- fast compile times
- minimal binary size
- no unnecessary allocations
- simple runtime behavior

As the project grows, record known performance requirements here.
